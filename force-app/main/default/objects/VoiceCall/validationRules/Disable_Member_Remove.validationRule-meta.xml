<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Disable_Member_Remove</fullName>
    <active>true</active>
    <description>Disables the Remove button on the &apos;Member&apos; component</description>
    <errorConditionFormula>AND(ISCHANGED( Contact__c  ), NOT(ISBLANK(PRIORVALUE( Contact__c ))), ISBLANK( Contact__c ))</errorConditionFormula>
    <errorMessage>Member cannot be blank for Voice Call.</errorMessage>
</ValidationRule>
